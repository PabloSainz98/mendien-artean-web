/* ============================================================
   MENDIEN ARTEAN — Plantilla de configuración
   Copia este archivo como js/config.js y rellena tus datos reales.
   config.js está en .gitignore y NO se sube al repositorio.
   ============================================================ */
window.SITE_CONFIG = {
  phone: '34XXXXXXXXX',              // para wa.me (sin + ni espacios)
  phoneDisplay: '+34 XXX XXX XXX',   // texto visible en la web
  email: 'tucorreo@ejemplo.com',
  apiBase: '/api',                   // endpoint backend para reservas
};
