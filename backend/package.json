{
  "name": "uxarbeiti-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Backend propio para reservas y disponibilidad",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/validation.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "nodemailer": "^10.0.1"
  }
}
